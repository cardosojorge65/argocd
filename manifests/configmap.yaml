apiVersion: v1
kind: ConfigMap
metadata:
  name: nodejs-config
  namespace: default
data:
  APP_ENV: production
  DB_HOST: db.production.example.com
  DB_PORT: "5432"
  DB_USER: admin
  DB_PASSWORD: securepassword
